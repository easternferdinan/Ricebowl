export const metadata = {
  title: "Ricebowl | Register"
}

export default function RegisterLayout({ children }) {
  return (
    <main className="flex justify-center items-center h-full m-0">
      {children}
    </main>
  );
}
