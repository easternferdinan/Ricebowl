export const metadata = {
  title: "Ricebowl | Login"
}

export default function LoginLayout({ children }) {


  return (
    <main className="flex justify-center items-center h-full m-0">
      {children}
    </main>
  );
}
